# Plan: Clear `src/app/app.html`

## Goal
Remove all existing content from `src/app/app.html`, leaving the file empty.

## Constraints
- Plan mode is active, so no implementation edits should be made until plan mode exits.
- Only the plan file may be written during planning.

## Implementation Steps
1. Read `src/app/app.html` if needed to confirm its current contents.
2. Replace the file contents with an empty string.
3. Verify `src/app/app.html` is empty.

## Expected Result
`src/app/app.html` contains no markup, styles, Angular template syntax, or `router-outlet`.
